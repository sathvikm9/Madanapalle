{
  "name": "@skct/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply sri-krishna-tracker --local",
    "migrate:remote": "wrangler d1 migrations apply sri-krishna-tracker --remote"
  },
  "dependencies": {
    "@skct/core": "1.0.0"
  },
  "devDependencies": {
    "wrangler": "4.124.0"
  }
}
